<!-- <h1>Cookbooks</h1>


< % @cookbooks.each do |cookbook| %>
  <ol>
    <h2>< %= link_to cookbook.title, cookbook_path(cookbook.id) %></h2>
    <div>< %= image_tag cookbook.image, :class => 'cookbook' %></div>
    <ul>
      <li>Description: < %= cookbook.description %></li>
    </ul>
  </ol>
< % end %> -->


  <div class= "cookbook">
    <h1>My Cookbooks</h1>
    <% @cookbooks.each do |cookbook| %>
      <div class="col-md-4">
        <div class="thumbnail">
          <div class="caption">
            <h2><%= link_to cookbook.title, cookbook_path(cookbook.id) %></h2>
            <p><%= cookbook.description %></p>
          </div>
        </div>
       </div>
     <% end %>
  </div>